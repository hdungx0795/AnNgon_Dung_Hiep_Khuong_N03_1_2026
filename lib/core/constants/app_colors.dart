import 'package:flutter/material.dart';

class AppColors {
  // Light Theme
  static const primary = Color(0xFFE53935);        // Đỏ mềm đặc trưng Phenikaa
  static const primaryDark = Color(0xFFC62828);
  static const secondary = Color(0xFFFF7043);       // Cam ấm
  static const background = Color(0xFFF5F5F5);      // Nền chính
  static const surface = Color(0xFFFFFFFF);         // Nền card
  
  // Text Colors
  static const textPrimary = Color(0xFF2C2F3E);     // Title
  static const textSecondary = Color(0xFF8A8D9F);   // Subtitle
  
  // Semantic Colors
  static const success = Color(0xFF4CAF50);
  static const warning = Color(0xFFFFA726);
  static const error = Color(0xFFEF5350);
  static const divider = Color(0xFFE0E0E0);
  
  // Dark Theme
  static const darkBackground = Color(0xFF121212);
  static const darkSurface = Color(0xFF1E1E1E);
  static const darkText = Color(0xFFE0E0E0);
}
