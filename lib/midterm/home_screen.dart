import 'package:flutter/material.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Giao diện Trang Chủ - Hiệp code ở đây',
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}
